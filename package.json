{
  "name": "websockets",
  "private": true,
  "scripts": {
    "start": "npm start --prefix server",
    "dev": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev:all": "concurrently -n api,web -c blue,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build:client": "npm install --prefix client && npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
